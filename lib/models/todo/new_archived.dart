import 'package:flutter/material.dart';
class NewArcived extends StatelessWidget {
  const NewArcived({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('new archived');
  }
}
